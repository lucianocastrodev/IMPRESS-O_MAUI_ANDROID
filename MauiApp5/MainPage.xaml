<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiApp5.MainPage"
             BackgroundColor="#F3F4F6"
             Title="Impressão ESC/POS">

    <Grid Padding="30">

        <!-- Card principal -->
        <Border
            StrokeThickness="0"
            BackgroundColor="White"
            Padding="25"
            HorizontalOptions="Fill"
            VerticalOptions="Center"
            StrokeShape="RoundRectangle 25">

            <Border.Shadow>
                <Shadow Brush="#999999"
                        Offset="0,1"
                        Radius="10" />
            </Border.Shadow>

            <VerticalStackLayout Spacing="25">

                <!-- Ícone -->
                <Image
                    Source="printer_icon.png"
                    HeightRequest="120"
                    Aspect="AspectFit"
                    HorizontalOptions="Center" />

                <!-- Título -->
                <Label
                    Text="Impressão via Bluetooth"
                    FontSize="26"
                    FontAttributes="Bold"
                    TextColor="#1F2937"
                    HorizontalOptions="Center" />

                <!-- Texto -->
                <Label
                    Text="Envie um comprovante de teste para a impressora térmica."
                    FontSize="16"
                    TextColor="#6B7280"
                    HorizontalTextAlignment="Center" />

                <!-- Botão moderno com gradiente -->
                <Border
                    StrokeThickness="0"
                    StrokeShape="RoundRectangle 14"
                    HeightRequest="55">

                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#6366F1" Offset="0.0" />
                            <GradientStop Color="#8B5CF6" Offset="1.0" />
                        </LinearGradientBrush>
                    </Border.Background>

                    <Button
                        x:Name="PrintTestBtn"
                        Text="Imprimir Teste"
                        Clicked="PrintTestBtn_Clicked"
                        BackgroundColor="Transparent"
                        TextColor="White"
                        FontSize="18"
                        CornerRadius="0" />
                </Border>

                <!-- Status -->
                <Label
                    x:Name="StatusLabel"
                    Text=""
                    FontSize="14"
                    TextColor="#6B7280"
                    HorizontalOptions="Center" />

            </VerticalStackLayout>
        </Border>

    </Grid>

</ContentPage>
